#!/usr/bin/env bash

set -e

shopt -s extglob

usage() {
    echo "Usage: $0 [NAME ...]" 1>&2
    echo "  Remove ccd directories not in the supplied list."
    exit 1
}

match=$(printf "|%s" "$@")
match=${match:1}

cd {{ vpn_ccd_dir }}

rm -f !("$match")
